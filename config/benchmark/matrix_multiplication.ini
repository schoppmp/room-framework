max_rows_server = 256
inner_dim = 5000000 # doesn't really matter, we're only testing sparse
max_cols_client = 1
max_nonzero_cols_server = 8192
max_nonzero_rows_client = 1024

pir_type = scs
pir_type = poly

statistical_security = 40

[server]
host=localhost
port=12347

[server]
host=localhost
port=12357
